version: 1.0
runtime: python3
build:
  commands:
    build:
      - echo "Using Dockerfile for build"
run:
  runtime: python3
  command: uvicorn app.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
    env:
      - name: PORT
        value: "8000"
